services:
  librion-db:
    image: postgres:16.4-alpine
    restart: always
    env_file:
      - path: .env
    ports:
      - "5678:5678"
    volumes:
      - librion-db:/var/lib/postgresql/data/

volumes:
  librion-db:
    driver: local
